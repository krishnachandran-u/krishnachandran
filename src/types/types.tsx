export type WorksTileProps = {
    heading: string,
    description: string,
    link: string,
};

export type WorksSwiperCardProps = {
    heading: string,
    description: string,
    link: string,
};